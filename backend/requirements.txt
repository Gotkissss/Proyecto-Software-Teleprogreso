# backend/requirements.txt
# Dependencias Python — Teleprogreso S.A.
# ──────────────────────────────────────────────────────────

# Framework web
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Base de datos
sqlalchemy==2.0.30
asyncpg==0.29.0
psycopg2-binary==2.9.9
alembic==1.13.1
geoalchemy2==0.15.1

# Autenticación JWT
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# Validación y configuración
pydantic==2.7.1
pydantic-settings==2.2.1
python-dotenv==1.0.1

# Utilidades
python-multipart==0.0.9
# Base de datos de zonas horarias: las imágenes -slim de Python no la traen y
# zoneinfo no puede resolver "America/Guatemala" sin ella.
tzdata==2024.1

#  Dependencias de prueba
# Instalar con: pip install -r requirements.txt
pytest==8.1.1
pytest-asyncio==0.23.6
httpx==0.27.0           